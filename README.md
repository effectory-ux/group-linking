# Group linking (ROC)

Standalone prototype of the **Previous survey links** flow (group linking), branded for ROC.

Live: https://effectory-ux.github.io/group-linking/

- `index.html` — the prototype (copied from `group-linking-bol.html` in the Engage docs repo, rebranded to ROC)
- `components.css`, `foundation.css`, `tokens.css`, `icons.js`, `assets/` — Effectory design system, vendored so the page runs standalone

The group structure in `index.html` is still the original tree from the source prototype and will be replaced with the ROC structure.
